"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Base = void 0;
const fetchImport = require("isomorphic-unfetch");
const types_1 = require("./types");
const fetch = (fetchImport.default || fetchImport);
class Base {
    constructor(config) {
        this.accessToken = config.accessToken || "";
        this.baseUrl = config.env === types_1.DhanEnv.PROD ? "https://api.dhan.co" : "https://api.dhan.co";
    }
    request(endpoint, options) {
        const url = `${this.baseUrl}${endpoint}`;
        const headers = {
            "Content-Type": "application/json",
            "access-token": this.accessToken,
        };
        const config = Object.assign(Object.assign({}, options), { headers });
        return fetch(url, config).then((response) => {
            return response.json();
        });
    }
}
exports.Base = Base;
