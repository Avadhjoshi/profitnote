<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sign Up | ProfitNote</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/alpinejs" defer></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
    input:-webkit-autofill {
      -webkit-box-shadow: 0 0 0 30px #1c1c1e inset !important;
      -webkit-text-fill-color: white !important;
    }
    [x-cloak] { display: none !important; }
    @keyframes slide { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
    .animate-slide { animation: slide 0.3s ease-out; }
  </style>
  </head>
<body class="bg-[#0f0f0f] text-white">

  <!-- Toast Error -->
  <% if (error && error.length > 0) { %>
    <div x-data="{ show: true }" x-init="setTimeout(() => show = false, 4000)" x-show="show"
      class="fixed top-5 right-5 bg-red-600 text-white text-sm px-4 py-2 rounded shadow-md z-50 animate-slide">
      <%= error %>
    </div>
  <% } %>

  <div class="min-h-screen flex flex-col lg:flex-row">
    <!-- Left -->
    <div class="w-full lg:w-1/2 flex items-center justify-center p-8">
      <div class="max-w-sm w-full">
        <h2 class="text-3xl font-bold mb-2">Sign Up</h2>
        <p class="text-gray-400 mb-6">Create a new account.</p>

        <form action="/signup" method="POST" class="space-y-4">
          <!-- Full Name -->
          <div>
            <label class="block mb-1 text-sm">Full Name</label>
            <div class="flex items-center bg-[#1c1c1e] px-3 py-2 rounded-md">
              <i class="fa-solid fa-user text-gray-400 mr-2"></i>
              <input type="text" name="registerName" required placeholder="John Doe"
                class="bg-transparent w-full text-sm text-white placeholder-gray-500 focus:outline-none" />
            </div>
          </div>

          <!-- Email -->
          <div>
            <label class="block mb-1 text-sm">Email</label>
            <div class="flex items-center bg-[#1c1c1e] px-3 py-2 rounded-md">
              <i class="fa-solid fa-envelope text-gray-400 mr-2"></i>
              <input type="email" name="registerEmail" required placeholder="example@gmail.com" autocomplete="off"
                class="bg-transparent w-full text-sm text-white placeholder-gray-500 focus:outline-none" />
            </div>
          </div>

          <!-- Password -->
          <div>
            <label class="block mb-1 text-sm">Password</label>
            <div class="flex items-center bg-[#1c1c1e] px-3 py-2 rounded-md">
              <i class="fa-solid fa-lock text-gray-400 mr-2"></i>
              <input type="password" name="registerPassword" required placeholder="••••••••" autocomplete="off"
                class="bg-transparent w-full text-sm text-white placeholder-gray-500 focus:outline-none" />
            </div>
          </div>

          <!-- Refer Code -->
          <div>
            <label class="block mb-1 text-sm">Refer Code (optional)</label>
            <div class="flex items-center bg-[#1c1c1e] px-3 py-2 rounded-md">
              <i class="fa-solid fa-tag text-gray-400 mr-2"></i>
              <input type="text" name="refer_code"
                value="<%= typeof refer !== 'undefined' ? refer : '' %>"
                <%= refer ? 'readonly' : '' %>
                placeholder="Enter refer code if any"
                class="bg-transparent w-full text-sm text-white placeholder-gray-500 focus:outline-none" />
            </div>
          </div>

          <button type="submit"
            class="w-full bg-green-600 hover:bg-green-700 text-white py-2 rounded-md font-medium">Register</button>

          <div class="relative my-4">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-gray-600"></div>
            </div>
            <div class="relative flex justify-center text-sm">
              <span class="bg-[#0f0f0f] px-2 text-gray-400">OR</span>
            </div>
          </div>

          <a href="/auth/google"
            class="flex items-center justify-center gap-3 bg-[#1c1c1e] py-2 w-full text-white rounded-md hover:bg-[#2a2a2e]">
            <img src="https://www.svgrepo.com/show/475656/google-color.svg" class="w-5 h-5" />
            Continue with Google
          </a>

          <p class="text-sm mt-4 text-right">
            Already have an account? <a href="/login" class="text-blue-500 hover:underline">Login</a>
          </p>
        </form>
      </div>
    </div>

    <!-- Right -->
    <div class="hidden lg:flex w-1/2 bg-[#121212] items-center justify-center p-8">
      <div class="text-center max-w-md">
        <img src="/uploads/illustration.png" alt="Chart" class="mx-auto w-64 mb-6">
        <h3 class="text-xl font-semibold mb-2">Welcome to ProfitNote</h3>
        <p class="text-gray-400 text-sm">
          Track and journal your trades with powerful analytics and smart insights.
        </p>
      </div>
    </div>
  </div>
</body>
</html>
